//Bayu Ilham
//065120067

//5. Menggunkan Dart untuk pemrograman fungsional
 
String scream(int length) => "A${'a' * length}h!";

main() {
  final values = [1, 2, 3, 5, 10, 50];
  for (var length in values) {
    values.skip(1).take(3).map(scream).forEach(print);
  }
}
